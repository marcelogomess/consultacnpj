apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cnpj-downloads-pvc
  namespace: cnpj
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard
