apiVersion: v1
kind: Secret
metadata:
  name: cnpj-api-secret
  namespace: cnpj
type: Opaque
stringData:
  # Substitua pela URL real em produção via Kustomize overlay ou gestão de secrets
  DATABASE_URL: "postgresql://user:pass@postgres:5432/cnpj?schema=public"
