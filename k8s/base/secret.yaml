apiVersion: v1
kind: Secret
metadata:
  name: cnpj-api-secret
  namespace: cnpj
type: Opaque
stringData:
  # Substitua pela URL real em produção via Kustomize overlay ou gestão de secrets (ex: Sealed Secrets, ESO)
  DATABASE_URL: "postgresql://user:pass@postgres:5432/cnpj?schema=public"
  # Token do compartilhamento público da Receita Federal (Nextcloud Basic-auth username)
  RECEITA_SHARE_TOKEN: "YggdBLfdninEJX9"
